extends layout

block content
  h1= title
  div(ng-app='app')    
      script(type='text/javascript').
        angular.module('app',[]);
      p Welcome to #{title}
      a(href='/fb/auth') click to log in to Facesmash
      br
      a(href='/userlist') see users